name: Deploy to Windows Server (MERN + IIS + PM2)

on:
  push:
    branches:
      - develop
      - testing
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Detect target environment
      - name: Detect target environment
        id: env
        run: |
          if [[ "${GITHUB_REF##*/}" == "develop" ]]; then
            echo "env=development" >> $GITHUB_OUTPUT
          elif [[ "${GITHUB_REF##*/}" == "testing" ]]; then
            echo "env=testing" >> $GITHUB_OUTPUT
          else
            echo "env=production" >> $GITHUB_OUTPUT
          fi

      # 3️⃣ Debug backend target path
      - name: Debug backend target path
        run: |
          echo "Backend target path: /c/github/ticketing-system/${{ steps.env.outputs.env }}/psba-ticket-management-portal/Backend"

      # 4️⃣ Upload backend folder (Windows-friendly)
      - name: Upload backend folder
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          source: "Ticketing-Management-System-Backend-/**"
          target: "/c/github/ticketing-system/${{ steps.env.outputs.env }}/psba-ticket-management-portal/Backend"  # 🔹 Unix-style /c/
          rm: true
          overwrite: true
          debug: true
          tar: false               # 🔹 DISABLE tar for Windows
          tar_dereference: true    # 🔹 Handle symlinks recursively
          timeout: 5m
          command_timeout: 10m

      # 5️⃣ Debug frontend target path
      - name: Debug frontend target path
        run: |
          echo "Frontend target path: /c/github/ticketing-system/${{ steps.env.outputs.env }}/psba-ticket-management-portal/Frontend"

      # 6️⃣ Upload frontend folder (Windows-friendly)
      - name: Upload frontend folder
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          source: "Ticketing-Management-System-Frontend/**"
          target: "/c/github/ticketing-system/${{ steps.env.outputs.env }}/psba-ticket-management-portal/Frontend"  # 🔹 Unix-style /c/
          rm: true
          overwrite: true
          debug: true
          tar: false               # 🔹 DISABLE tar for Windows
          tar_dereference: true    # 🔹 Handle symlinks recursively
          timeout: 5m
          command_timeout: 10m

      # 7️⃣ Run remote PowerShell deploy script
      - name: Run remote PowerShell deploy script
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          script: |
            powershell -NoProfile -ExecutionPolicy Bypass -File "C:/github/ticketing-system/deploy_scripts/deploy.ps1" -env ${{ steps.env.outputs.env }}